# Configuración de entorno - Economía Circular Canarias
# Copia este archivo como .env y configura tus valores reales

# Entorno de desarrollo/producción
ENVIRONMENT=development

# Configuración de base de datos
DB_HOST=localhost
DB_NAME=tu_base_de_datos
DB_USER=tu_usuario
DB_PASS=tu_contraseña_segura
DB_CHARSET=utf8mb4
DB_PORT=3306

# Configuración de JWT (generar una clave segura para producción)
# Puedes usar: openssl rand -base64 32
JWT_SECRET=tu_clave_secreta_jwt_muy_segura

# Configuración de CORS
CORS_ORIGIN=http://localhost:8080

# Configuración de cookies
COOKIE_NAME=canarias_auth_token

# Rate limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# INSTRUCCIONES:
# 1. Copia este archivo como .env
# 2. Reemplaza todos los valores con tus configuraciones reales
# 3. NUNCA subas el archivo .env a repositorios públicos
# 4. Para generar JWT_SECRET seguro: openssl rand -base64 32
