RewriteEngine On
RewriteBase /Nueva-WEB/api/
# Si el archivo o directorio existe, no reescribir
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Redirige todo lo demás a index.php
RewriteRule ^(.*)$ index.php [QSA,L]
